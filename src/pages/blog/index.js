import * as React from 'react'
import Layout from '../../components/layout'

const BlogPage = ({location}) => {
  return <Layout pageTitle="Blog" location={location}></Layout>
}

export default BlogPage