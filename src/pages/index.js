import * as React from "react";
import Layout from "../components/layout";

const IndexPage = ({ location }) => {
  return <Layout pageTitle="Desarrollador Web" location={location}></Layout>;
};

export default IndexPage;
